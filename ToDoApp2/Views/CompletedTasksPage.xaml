<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:ToDoApp2.Models"
             x:Class="ToDoApp2.Views.CompletedTasksPage"
             Title="Completed Tasks"
             BackgroundColor="#FFECC0">

    <ScrollView>
        <VerticalStackLayout Padding="20">

            <!-- Two Buttons at Top -->
            <HorizontalStackLayout HorizontalOptions="Center"
                                   Spacing="20"
                                   Margin="0,10,0,5">
                <Button Text="← Back"
                        BackgroundColor="#FFB997"
                        TextColor="Black"
                        CornerRadius="25"
                        FontSize="16"
                        WidthRequest="160"
                        Padding="15,8"
                        Clicked="BackButtonClicked"/>
                <Button Text="X Tasks"
                        BackgroundColor="#A1E17F"
                        TextColor="Black"
                        CornerRadius="25"
                        FontSize="16"
                        WidthRequest="160"
                        Padding="15,8"
                        Clicked="UncompletedButtonClicked"/>
            </HorizontalStackLayout>

            <Label Text="✓ Completed Tasks"
                   FontSize="24"
                   HorizontalOptions="Center"
                   Margin="0,10"
                   TextColor="Black"/>

            <CollectionView ItemsSource="{Binding CompletedTasks}">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="10" />
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Task">
                        <Frame BorderColor="#D1B070"
                               CornerRadius="10"
                               Padding="10"
                               Margin="0,5"
                               BackgroundColor="#C8E6C9">
                            <VerticalStackLayout Spacing="2">
                                <Label Text="{Binding Title}"
                                       FontAttributes="Bold"
                                       FontSize="18"
                                       TextColor="Black" />
                                <Label Text="{Binding DueDate, StringFormat='Due: {0:dd/MM/yyyy}'}"
                                       FontAttributes="Italic"
                                       FontSize="14"
                                       TextColor="Black" />
                            </VerticalStackLayout>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>

                <CollectionView.EmptyView>
                    <VerticalStackLayout Padding="20" HorizontalOptions="Center" VerticalOptions="Center">
                        <Label Text="No completed tasks yet" FontSize="16" />
                    </VerticalStackLayout>
                </CollectionView.EmptyView>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
